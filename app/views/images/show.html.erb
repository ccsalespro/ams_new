<p id="notice"><%= notice %></p>

<p>
  <strong>Filename:</strong>
  <%= @image.filename %>
</p>

<p>
  <strong>Content type:</strong>
  <%= @image.content_type %>
</p>

<%= image_url(@image.filename) %><br><br>
<img src="<%= url_for(:controller => "images", 
                      :action => "show", 
                      :id => @image.id) %>" />


<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
