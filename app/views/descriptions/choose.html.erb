
<%= search_form_for @search, url: choose_descriptions_path do |f| %>

      <div class="col-xs-3">
      <div class="form-inline">
      <%=f.search_field :business_type_primary_or_business_type_secondary_cont, :class => "form-control", :id => "inputdefault" %>
      <%= button_tag("Search", :class => 'btn btn-success') %>
      </div>
      </div>
<% end %>
<br><br><br><br>
 <div class="container-fluid">
   <table class="table table-hover table-striped">
    <% @descriptions.each do |description| %>
    <tr>
    <td><%= description.business_type_primary %> | </b><%= description.business_type_secondary %></td>
      <td class="text-right"><%= link_to(content_tag(:button, 'Choose', :class => 'btn btn-success'), new_description_prospect_path(description_id: description.id)) %></td>
    </tr>
    <% end %>
      </table>
</div>

