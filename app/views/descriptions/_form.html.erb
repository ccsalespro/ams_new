<%= form_for(@description) do |f| %>
  <% if @description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@description.errors.count, "error") %> prohibited this description from being saved:</h2>

      <ul>
      <% @description.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amex_business_type %><br>
    <%= f.select(:amex_business_type, options_for_select([['Retail', 'Retail'], ['Restaurant', 'Restaurant'], ['Other', 'Other']]), :class => 'form-control') %>
  </div>
  <div class="field">
    <%= f.label :business_type_primary %><br>
    <%= f.text_field :business_type_primary, :class => 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :business_type_secondary %><br>
    <%= f.text_field :business_type_secondary, :class => 'form-control' %>
  </div>
  <div class="actions">
    <%= submit_tag("Submit", :class => 'btn btn-default') %>
  </div>
<% end %>

