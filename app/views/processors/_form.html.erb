<%= form_for(@processor) do |f| %>
  <% if @processor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@processor.errors.count, "error") %> prohibited this processor from being saved:</h2>

      <ul>
      <% @processor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field"><br>
    <div class="input-group">
      <%= f.text_field :name, class: 'form-control', placeholder: 'New Processor', required: true %> <div class="input-group-addon"><%= f.submit "Save", class: "processor-new bold" %></div>
    </div>
  </div>
<% end %>
<br>
