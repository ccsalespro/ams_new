<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Get the training and technology solutions you need to sell small business owners">
    <meta name="author" content="James Shepherd">
	  <title>CCSalesPro</title>
        <%= stylesheet_link_tag('application', :media => 'all') %>
        <%= javascript_include_tag "application" %>
        <%= csrf_meta_tags %>
	</head> 
<div class="main-body">
<body>
<% flash.each do |name, msg| %>
  <%= content_tag(:div, msg, class: "alert alert-#{name}") %>
<% end %>
  
  <% if user_signed_in? %>
  <header>
  <div class="container">
    
    <%= link_to(content_tag(:button, 'Profile', :class => 'btn btn-primary'), edit_user_registration_path) %>
    <%= link_to(content_tag(:button, 'Sign Out', :class => 'btn btn-primary'), destroy_user_session_path, :method => :delete) %>
    <br><br>
  </div>
  </header>
  <% else %>
  <header>
  <div class="container">
   
    <%= link_to(content_tag(:button, 'Sign Up', :class => 'btn btn-primary'), new_user_registration_path) %>
    <%= link_to(content_tag(:button, 'Sign In', :class => 'btn btn-primary'), new_user_session_path) %>
    <br><br>
  </div>
  </header>
  <% end %>

<%= yield %>

</body>
</div>
</html>