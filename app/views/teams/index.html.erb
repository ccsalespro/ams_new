<div class="container text-center"><br />
  <div class="well welly">

    <%= link_to 'Create New Team', new_team_path, class: 'btn btn-lg btn-primary' %><br /><br />

    <table class="table table-striped table-responsive">
      <thead>
        <tr>
          <th class="text-center">Team Name</th>
          <th class="text-center">Type</th>
          <th colspan="2"></th>
        </tr>
      </thead>

      <tbody>
        <% @teams.each do |team| %>
        <% @team_user = TeamUser.where(user_id: current_user.id).where(team_id: team.id).first %>
        <% @manage_ids = [2, 1, 4, 7, 8, 9] %>
          <tr>
            <td><%= team.name %></td>
            <td><%= team.team_type.description %></td>
            <% if @manage_ids.include? @team_user.team_user_role_id %>
              <td><%= link_to 'Manage', team, class: 'btn btn-primary' %></td>
              <td><%= link_to 'Destroy', team, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-primary' %></td>
            <% else %>
              <td><%= link_to 'Home Page', team, class: 'btn btn-primary' %></td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
